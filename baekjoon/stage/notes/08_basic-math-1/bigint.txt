BigInt
    2^53 - 1 보다 큰 정수를 표현할 때 사용한다.
    *Number로는 표현할 수 없다.
    정수 뒤에 n을 붙이거나 BigInt()로 호출해 생성한다.
    BigInt 끼리 +, *, -, **, % 연산할 수 있다.
    단항 + 연산자는 사용할 수 없다.
    >>>는 사용할 수 없다. /는 소수점 이하를 버린다.
    다른 타입의 자료형과 연산할 수 없다.